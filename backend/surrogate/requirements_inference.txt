# 代理模型推理服务依赖
# 用于生产化部署，不依赖完整 PyTorch 训练环境

# ---------- 核心推理依赖（必需）----------
# 选择其中一个即可：

# 选项 1: ONNX Runtime (推荐，轻量高性能)
onnxruntime>=1.16.0
# 或 GPU 版本:
# onnxruntime-gpu>=1.16.0

# 选项 2: TorchScript (如果使用 TorchScript 格式)
# torch>=2.0.0

# ---------- 推理服务依赖（必需）----------
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# ---------- 客户端依赖（可选）----------
requests>=2.31.0

# ---------- 导出时可选依赖（训练端）----------
# onnx>=1.15.0
# onnxsim>=0.4.0
