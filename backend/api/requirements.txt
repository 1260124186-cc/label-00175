# ============================================================
# 后端 Web API 运行依赖（FastAPI + 仿真核心）
# Python 3.10 / 3.11 完全兼容；CI/CD/Docker 构建稳定
# ============================================================

# ---------- Web 框架 ----------
fastapi>=0.104.0,<1.0
uvicorn[standard]>=0.24.0,<1.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0
python-multipart>=0.0.6,<1.0

# ---------- 核心数值计算（仿真需要） ----------
numpy>=1.24.0,<2.0
scipy>=1.10.0,<2.0
numba>=0.58.0,<1.0

# ---------- 图像处理（utils.data_io 中使用，惰性导入但 API 仿真会用到） ----------
opencv-python-headless>=4.8.0
Pillow>=10.0.0,<11.0

# ---------- 配置 / 网络 ----------
PyYAML>=6.0.1,<7.0
httpx>=0.25.0,<1.0

# ---------- 可视化（供将来结果导出使用） ----------
matplotlib>=3.7.0,<4.0
