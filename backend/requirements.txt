# 计算光刻仿真框架依赖
# Python 3.9+

# 核心数值计算
numpy>=1.21.0
scipy>=1.7.0

# 图像处理
opencv-python>=4.5.0
Pillow>=8.0.0

# 可视化
matplotlib>=3.4.0

# 性能加速
numba>=0.54.0

# 配置文件
PyYAML>=5.4.0

# 测试
pytest>=6.2.0
pytest-cov>=2.12.0

# 日志
colorlog>=5.0.0

# 版图文件解析（GDS/OASIS导入，二者安装其一即可）
gdstk>=0.9.0

# HDF5 批量存储
h5py>=3.0.0

# 资源监控（自动检测 CPU/内存）
psutil>=5.8.0

# ---------------------------------------------------------------------------
# 可选：批处理分布式调度（Celery + Redis）
#   本地多进程模式（默认）不需要以下依赖。
#   启用分布式集群时：pip install -r requirements.txt celery redis
# ---------------------------------------------------------------------------
# celery>=5.3.0
# redis>=4.5.0
