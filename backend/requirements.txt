# 计算光刻仿真框架依赖
# Python 3.10 / 3.11 验证通过

# ---------- 核心数值计算 ----------
numpy>=1.24.0,<2.0
scipy>=1.10.0,<2.0
numba>=0.58.0,<1.0

# ---------- 图像处理 ----------
opencv-python>=4.8.0
Pillow>=10.0.0,<11.0

# ---------- 可视化 ----------
matplotlib>=3.7.0,<4.0
plotly>=5.18.0,<6.0

# ---------- 配置文件 ----------
PyYAML>=6.0.1,<7.0

# ---------- 测试 ----------
pytest>=7.4.0
pytest-cov>=4.1.0

# ---------- 日志 ----------
colorlog>=6.7.0

# ---------- 版图文件解析（GDS/OASIS导入，二者安装其一即可） ----------
# 注意：gdstk 需要 cmake + build-essential，Docker 中会自动安装；
#       纯 Python 环境如无法编译可跳过（utils.data_io 中是惰性导入）
gdstk>=0.9.42

# ---------- HDF5 批量存储 ----------
h5py>=3.9.0

# ---------- 资源监控（自动检测 CPU/内存） ----------
psutil>=5.9.0

# ---------------------------------------------------------------------------
# 批处理分布式调度（Celery + Redis）
#   本地多进程模式也可安装，用于开发调试分布式功能
# ---------------------------------------------------------------------------
celery>=5.3.0,<6.0
redis>=4.5.0,<6.0

# ---------------------------------------------------------------------------
# 可选：神经网络代理模型（Surrogate Model），用于加速 OPC/SMO 前向仿真
#   不使用代理模型时（默认）不需要以下依赖。
#   启用 use_surrogate=True 前请先安装：
#       pip install torch torchvision scikit-image
#   注意：
#     - torch 版本请根据 CUDA / MPS / CPU 环境从 https://pytorch.org 选择
#     - Apple Silicon (M1/M2/M3) 用户建议使用 conda 安装 pytorch 以启用 MPS
# ---------------------------------------------------------------------------
# torch>=2.0.0              # 神经网络框架（必需）
# torchvision>=0.15.0       # 图像处理工具（推荐）
# scikit-image>=0.21.0      # SSIM/PSNR 评估指标（推荐）
