# ========================================================================
# 产线推理微服务 - Python 依赖清单
# ========================================================================
# 双模式支持:
#   1. 最小部署 (Hopkins 近似 + HTTP): 仅需 numpy + fastapi
#   2. 代理模型部署: 额外需要 onnxruntime 或 torch
#   3. 完整部署: gRPC + HTTP + 双引擎
# ========================================================================

# ---------- 核心依赖 (必需) ----------
numpy>=1.22.0

# ---------- 推理引擎 (可选, 至少选一个) ----------
# 选项 A: ONNX Runtime (推荐代理模型部署
onnxruntime>=1.16.0
# GPU 版本替换为:
# onnxruntime-gpu>=1.16.0

# 选项 B: PyTorch TorchScript
# torch>=2.0.0

# ---------- HTTP/REST 服务 ----------
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# ---------- gRPC 服务 (可选) ----------
grpcio>=1.59.0
grpcio-tools>=1.59.0
grpcio-reflection>=1.59.0

# ---------- 配置加载 ----------
PyYAML>=6.0.1

# ---------- 客户端调用 ----------
requests>=2.31.0

# ---------- 监控与性能 (可选) ----------
# prometheus-client>=0.19.0
# psutil>=5.9.0
